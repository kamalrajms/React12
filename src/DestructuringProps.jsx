import React from "react";

export default function DestructuringProps({ address, number }) {
  return (
    <div>
      <h2>Address:{address}</h2>
      <h2>ph number:{number}</h2>
    </div>
  );
}
